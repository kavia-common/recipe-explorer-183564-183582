# Example environment variables for future configuration
# JWT secret key (if JWT auth is introduced later)
JWT_SECRET=replace-with-secure-random-string
# Token expiration in seconds
JWT_EXPIRES_IN=3600
