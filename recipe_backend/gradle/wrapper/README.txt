This directory contains Gradle wrapper metadata.

The actual gradle-wrapper.jar is not committed here and will be downloaded automatically
from the distribution URL specified in gradle-wrapper.properties when you invoke ./gradlew
for the first time in the CI/preview environment.
