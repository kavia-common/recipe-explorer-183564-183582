spring:
  application:
    name: recipe-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

server:
  port: 3001
  address: 0.0.0.0
